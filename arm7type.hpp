#ifndef __ARM7TYPE_HPP__
#define __ARM7TYPE_HPP__

using u8arm_t = unsigned char;
using i8arm_t = char;

using u16arm_t = unsigned short;
using i16arm_t = short;

using u32arm_t = unsigned int;
using i32arm_t = int;

#endif
